--- a/utils/dataframe_utils.py
+++ b/utils/dataframe_utils.py
@@
+from typing import Tuple
+import pandas as pd
+
+# helper local para reuso de filtro por período
+def _filtrar_periodo(df: pd.DataFrame, di, dfim) -> pd.DataFrame:
+    if df is None or df.empty or "Data" not in df.columns:
+        return pd.DataFrame()
+    return df[(df["Data"] >= di) & (df["Data"] <= dfim)]
@@
-def resolver_base_ponto(df_ponto, df_relacao, nome_boletim, data_ini, data_fim, corte_similaridade=0.75):
+def resolver_base_ponto(df_ponto, df_relacao, nome_boletim, data_ini, data_fim, corte_similaridade=0.75):
@@
-    # ... lógica existente ...
+    # Proteções iniciais
+    if df_ponto is None or not isinstance(df_ponto, pd.DataFrame) or df_ponto.empty:
+        return pd.DataFrame(), "INEXISTENTE", None
+
+    # Converte datas uma vez
+    di = pd.to_datetime(data_ini)
+    dfim = pd.to_datetime(data_fim)
+
+    # Garante Data em datetime
+    if "Data" in df_ponto.columns and not pd.api.types.is_datetime64_any_dtype(df_ponto["Data"]):
+        with pd.option_context("mode.chained_assignment", None):
+            df_ponto["Data"] = pd.to_datetime(df_ponto["Data"], dayfirst=True, errors="coerce")
+            df_ponto = df_ponto[df_ponto["Data"].notna()].copy()
+
+    # Índices auxiliares (se disponíveis)
+    idx_cpf = df_ponto.set_index("CPF") if "CPF" in df_ponto.columns else None
+    idx_pis = df_ponto.set_index("PIS") if "PIS" in df_ponto.columns else None
+
+    # 1) Tentativa via df_relacao
+    if df_relacao is not None and not df_relacao.empty and "Nome_Boletim" in df_relacao.columns:
+        rel = df_relacao[df_relacao["Nome_Boletim"] == nome_boletim]
+        if not rel.empty:
+            r = rel.iloc[0]
+            cpf = str(r.get("CPF_Ponto") or "").strip()
+            pis = str(r.get("PIS_Ponto") or "").strip()
+            nome_map = str(r.get("Nome_Ponto_Mapeado") or "").strip()
+
+            if cpf and idx_cpf is not None and cpf in idx_cpf.index:
+                base = idx_cpf.loc[[cpf]].reset_index()
+                return _filtrar_periodo(base, di, dfim), "CPF", cpf
+            if pis and idx_pis is not None and pis in idx_pis.index:
+                base = idx_pis.loc[[pis]].reset_index()
+                return _filtrar_periodo(base, di, dfim), "PIS", pis
+            if nome_map and "Nome" in df_ponto.columns:
+                mask = df_ponto["Nome"].astype(str).str.casefold() == nome_map.casefold()
+                if mask.any():
+                    base = df_ponto[mask]
+                    return _filtrar_periodo(base, di, dfim), "NOME_MAPEADO", nome_map
+
+    # 2) Fallback por similaridade de nome (mantém sua lógica corrente)
+    if "Nome" in df_ponto.columns:
+        from difflib import get_close_matches
+        nomes = df_ponto["Nome"].dropna().astype(str).unique().tolist()
+        alvos = [n.casefold() for n in nomes]
+        match = get_close_matches(str(nome_boletim).casefold(), alvos, n=1, cutoff=corte_similaridade)
+        if match:
+            nome_real = next(n for n in nomes if n.casefold() == match[0])
+            base = df_ponto[df_ponto["Nome"] == nome_real]
+            return _filtrar_periodo(base, di, dfim), "SIMILARIDADE", nome_real
+
+    return pd.DataFrame(), "INEXISTENTE", None